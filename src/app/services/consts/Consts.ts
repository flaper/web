import {Config} from '../../config/Config';
export const API_SERVER_URL = `http://${Config.api.host}:${Config.api.port}`;
export const API_BASE_URL = `http://${Config.api.host}:${Config.api.port}/api/`;

